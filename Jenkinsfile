pipeline {
    agent any

    tools {
        maven 'mvn-3.9.6'
    }

    stages {
        stage('Build') {
            steps {
                echo "test github webhood trigger jenkins"
                sh "printenv"
            }
        }
    }
}
